# freenime

Self-hosted video streaming starter.

## Start
1. Install Node.js 18+.
2. Open a terminal in this folder.
3. Run `npm install`.
4. Set an admin key:
   Windows PowerShell: `$env:ADMIN_KEY="your-secret-key"`
   macOS/Linux: `export ADMIN_KEY="your-secret-key"`
5. Run `npm start`.
6. Open http://localhost:3000
7. Upload at http://localhost:3000/admin.html

Only use content you own or have permission/licensing to distribute.
